language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-globeproject' ]; then cd .. && eval "mv $currentfolder generator-globeproject" && cd generator-globeproject; fi

